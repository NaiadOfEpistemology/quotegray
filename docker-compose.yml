version: "3.9"

services:
  ai:
    build: ./ai-engine
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434/api/generate
  frontend: 
    build: ./frontend
    ports: 
      - "5173:5173"
    depends_on:
      - ai